/**
 * Platform components — barrel export
 *
 * Generic rendering primitives used by route-level page components.
 * Import from "@/components/platform" rather than from the individual files.
 *
 *   import { TemplateRenderer }    from "@/components/platform";
 *   import { ContentBlockRenderer } from "@/components/platform";
 */

export { TemplateRenderer }     from "./TemplateRenderer";
export { ContentBlockRenderer } from "./ContentBlockRenderer";
